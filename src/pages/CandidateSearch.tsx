import { useState, useEffect } from 'react';
import { searchGithub, searchGithubUser } from '../api/API';

const CandidateSearch = () => {
  return <h1>CandidateSearch</h1>;

  // TODO: Add code to display one candidate.
  // TODO: Add code so that when the "+" is clicked, the current candidate is saved to the Saved Candidates page. Then display the next candidate.
  // TODO: Add code so that when the "-" is clicked, the current candidate is skipped and the next candidate is displayed.
  // TODO: Add code so that when the list of available candidates is exhausted, a message is displayed to the user.
  // TODO: Add code so when the potential candidates load, the user sees a list of previously saved candidates.



};

export default CandidateSearch;
